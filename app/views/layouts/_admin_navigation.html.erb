<nav class="navbar navbar-expand-lg navbar-dark bg-dark justify-content-end">
  <div class="container-fluid">
    <%= link_to 'Test-app', root_path, class: 'navbar-brand' %>
    <%= content_tag :p, 'Admin', class: 'navbar-brand m-0' %>
    <ul class="nav justify-content-end">
      <li class="nav-item">
        <%= content_tag :p, t('greetings', user: current_user.name), class: 'navbar-text m-0' %>
      </li>
      <li class="nav-item">
        <%= button_to t('logout'), destroy_user_session_path, method: :delete, class: 'btn btn-outline-light mx-2' %>
      </li>
    </ul>
  </div>
</nav>
