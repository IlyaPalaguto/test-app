<nav class="navbar navbar-expand-lg bg-light justify-content-end">
  <div class="container-fluid">
    <%= link_to 'Test-app', root_path, class: 'navbar-brand' %>
    <ul class="nav justify-content-end">
      <% if user_signed_in? %>
        <%= link_to t('admin_panel'), admin_root_path, class: 'navbar-brand' if current_user.is_a?(Admin)%>
        <li class="nav-item">
          <%= content_tag :p, t("greetings", user: current_user.name), class: 'navbar-text m-0' %>
        </li>
        <li class="nav-item">
          <%= button_to t('logout'), destroy_user_session_path, method: :delete, class: 'btn btn-outline-dark mx-2' %>
        </li>
      <% else %>
        <li class="nav-item">
          <%= link_to t('signup'), new_user_registration_path, class: 'nav-link' %>
        </li>
        <li class="nav-item">
          <%= link_to t('login'), new_user_session_path, class: 'nav-link' %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
