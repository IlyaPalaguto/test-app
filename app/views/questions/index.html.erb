<ul><% @questions.each {|question| %> 
      <li>
        <a href="<%= question_path(question) %>"><%= question.body %></a>
        <%= button_to 'Удалить', question_path(question), method: :delete %>
      </li>
  <% } %> 
</ul>
